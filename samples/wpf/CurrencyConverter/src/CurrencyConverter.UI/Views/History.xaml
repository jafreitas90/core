<UserControl x:Class="CurrencyConverter.UI.Views.History"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Background="Red">
        <ListView x:Name="HistoryListView" ItemsSource="{Binding Items}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="From"
                                    Width="120"
                                    DisplayMemberBinding="{Binding From}" />
                    <GridViewColumn Header="To"
                                    Width="120"
                                    DisplayMemberBinding="{Binding To}" />
                    <GridViewColumn Header="Result"
                                    Width="120"
                                    DisplayMemberBinding="{Binding Result}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>